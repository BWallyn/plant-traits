{
  "code": "def add_path_to_images(df: pd.DataFrame, path_images: str) -> pd.DataFrame:\n    \"\"\"Add path to each image in the dataframe\n\n    Args:\n        df (pd.DataFrame): Input dataframe\n        path_images (str): Path to the folder containing images\n    Returns:\n        df (pd.DataFrame): Output dataframe with path to images\n    \"\"\"\n    df['file_path'] = df['id'].apply(lambda s: f'{path_images}/{s}.jpeg')\n    return df\n",
  "filepath": "plant-traits/src/plant_traits/pipelines/prepare_data/nodes.py",
  "parameters": {
    "prepare_data.path_images": "data/03_primary/train_images/"
  },
  "run_command": "kedro run --to-nodes='prepare_data.add_path_to_images'",
  "inputs": [
    "df_raw",
    "params:prepare_data.path_images"
  ],
  "outputs": [
    "prepare_data.df_w_path"
  ]
}